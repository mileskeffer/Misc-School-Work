#include <iostream>
#include <fstream>
using namespace std;

int main(int argc, char ** argv){
    if (argc < 3) /* or, != to 3*/

}

/*
-open both files (infile1, infile2)
-check if files are open, if not, output error

-start reading files via: bool getline(infile1, var)
    -this will return either a false when it reaches the end of the file




-make check_diff
    string check_diff(int line_number, string l1, string l2)
        if l1 and l2


*/