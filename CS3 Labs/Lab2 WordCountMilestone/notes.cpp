using namespace std;
class myList{
    public:


    private:

};

int main(){

    //tale file as param
    //read the file
    //implement a way to separate the words
    //

        //std::sort() //need to override the < operator for the bools
        //selection sort
            /*      int lowest-idx = -1
                    MyWord lowest_myWord;
                    for (int i = 0; i < 10; i++){
                        if(word[i] < lowest_myWord){
                            lowest_myWord = word[i];
                            lowest-idx = i;
                        }
                    }
                    swap(word[0], word[lowest-idx]);

            */
}
